const multiply = (a, b) => a * b;

const divide = (a, b) => a / b;

exports.multiply = multiply;
exports.divide = divide;

// Syntaxes to export values in node.js

// Export single value
// module.exports = value;

// Export multiple values
// exports.foo = foo;
// exports.bar = bar;
